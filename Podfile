# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'legal_talk' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'AWSAppSync'
  # Pods for legal_talk
    pod 'AWSMobileClient', '~> 2.13.0'      # Required dependency
  pod 'AWSAuthUI', '~> 2.13.0'            # Optional dependency required to use drop-in UI
  pod 'AWSUserPoolsSignIn', '~> 2.13.0'   # Optional dependency required to use drop-in UI
  
  pod 'lottie-ios'
  pod 'IQKeyboardManager'
  pod 'AWSPinpoint'
  pod 'AWSMobileClient'
  pod 'FSCalendar'
  target 'legal_talkTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
